<h1>Recipes Index</h1>
<% @recipes.each do |recipe| %>
  <p><%= recipe.name %> with <%= recipe.ingredients.count %> ingredients</p>
  <br>
<% end %>

<%= form_tag("/sorted", method: "get") do %>
  <%= submit_tag("Sort") %>
<% end %>